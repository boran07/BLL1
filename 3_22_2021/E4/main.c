#include<stdio.h>
enum ABC{ A, B, C };


int main ()
    {
        enum ABC x;
        x = A + B + C;
        printf ("\n x = %d", x);
        printf ("\n %d %d %d", A, B, C);
        return 0;

    }
