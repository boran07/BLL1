#include <stdio.h>
#include <stdlib.h>



int main()
{

 //ptr  = (Tip*) malloc(size);

  //ptr = (float*) malloc(100* sizeof(float)); //400 bytelik

  //calloc
  //ptr  = (Tip*) calloc(n,size);


  //ptr = (float*) calloc (25, sizeof(float));

  //free(ptr);


}
